# 窮鬼地圖・階段一部署說明（給非工程師看的版本）

這份文件會一步步帶你把「窮鬼地圖」從程式碼變成一個真的可以打開的網址。整個過程大約 20〜30 分鐘，不需要寫任何程式碼，跟著點滑鼠、複製貼上就好。

專案資料夾裡有這幾個檔案，等一下會用到：

- `index.html`、`app.js` — 網站本身（不用動）
- `config.js` — 需要你貼上兩個 Supabase 的值（步驟三會說明）
- `supabase-schema.sql` — 要貼到 Supabase 執行一次的建表指令

---

## 步驟一：建立 Supabase 專案（資料庫）

1. 打開 https://supabase.com ，用 GitHub 或 Email 註冊 / 登入。
2. 登入後點「New Project」。
3. 填寫：
   - **Name**：例如 `qiongui-map`（隨意）
   - **Database Password**：設一組密碼並存起來（之後不太會用到，但要記得）
   - **Region**：選 `Northeast Asia (Tokyo)` 或離台灣最近的選項
4. 點「Create new project」，等待約 1〜2 分鐘讓 Supabase 建好環境。

---

## 步驟二：執行資料表建立指令

1. 專案建好後，左側選單點 **「SQL Editor」**。
2. 點右上角 **「New query」**。
3. 打開專案資料夾裡的 `supabase-schema.sql`，把整份內容複製貼到編輯區。
4. 點右下角 **「Run」**（或按 Ctrl/Cmd + Enter）。
5. 看到下方出現 `Success. No rows returned` 就代表建立成功。
6. 可以到左側「Table Editor」確認有出現 `restaurants`、`likes`、`confirmations`、`reports_flag` 四張表。

---

## 步驟三：打開匿名登入功能

因為使用者打開網站不需要註冊/登入，我們用的是 Supabase 的「匿名登入」：

1. 左側選單點 **「Authentication」** → 上方 **「Providers」**。
2. 找到 **「Anonymous Sign-Ins」**，把它切成 **啟用（Enabled）**。
3. 存檔（通常切換開關後會自動生效，若有 Save 按鈕記得按）。

---

## 步驟四：把金鑰貼進 config.js

1. 左側選單點 **「Project Settings」**（齒輪圖示）→ **「API」**。
2. 你會看到：
   - **Project URL**（一串 `https://xxxx.supabase.co`）
   - **Project API keys** 底下的 **anon public**（一長串英數字）
3. 打開專案資料夾裡的 `config.js`，把這兩個值分別貼進去：

```js
window.SUPABASE_URL = "貼上你的 Project URL";
window.SUPABASE_ANON_KEY = "貼上你的 anon public key";
```

4. 存檔。

> 這個 anon key 是「公開」用的，放在前端程式碼裡是 Supabase 官方建議的正常用法，真正保護資料的是資料庫的 Row Level Security（我們在步驟二的 SQL 已經設定好了）。

---

## 步驟五：把專案放到 GitHub

1. 打開 https://github.com ，登入你的帳號（沒有的話先免費註冊）。
2. 點右上角「+」→「New repository」。
3. **Repository name** 填 `qiongui-map`（或你喜歡的名字），設為 **Public** 或 **Private** 皆可，不要勾選「Add a README file」。
4. 點「Create repository」。
5. 依照 GitHub 顯示的指示，把本機這個資料夾推上去。如果你不熟悉指令，最簡單的方式是：
   - 在 GitHub 該頁面找到「uploading an existing file」的連結
   - 把資料夾內所有檔案（`index.html`、`app.js`、`config.js`、`supabase-schema.sql`、`.gitignore`）拖曳上傳
   - 下方填寫 commit message（例如「初版部署」），點「Commit changes」

---

## 步驟六：部署到 Vercel

1. 打開 https://vercel.com ，用 GitHub 帳號登入（會自動連動）。
2. 登入後點「Add New...」→「Project」。
3. 在列表中找到剛剛建立的 `qiongui-map` repo，點「Import」。
4. Framework Preset 選 **「Other」**（因為這是純 HTML 靜態網站，不需要 build），其他設定保持預設。
5. 點「Deploy」，等待約 30 秒〜1 分鐘。
6. 部署完成後，Vercel 會給你一個網址，例如 `https://qiongui-map.vercel.app`，這就是可以公開分享的網站！

---

## 步驟七：實際測試

打開部署好的網址，確認：

- [ ] 地圖能正常顯示，右下角「＋」可以打開回報表單
- [ ] 填寫店名、招牌品項、價格（試試填超過 200，應該會被擋下）、評分、地圖選位置後，按「蓋章送出」
- [ ] 送出後畫面上會出現剛剛新增的卡片，地圖上也有新的圖釘
- [ ] 重新整理頁面，剛剛新增的資料還在（代表真的存進 Supabase 了，不是暫存）
- [ ] 到 Supabase 的「Table Editor」→ `restaurants` 表，也能看到這筆新資料

全部打勾就代表階段一大功告成！

---

## 之後要怎麼改網站內容？

之後想調整文字、樣式、功能，直接把你想要的需求用中文描述給 Claude Code，我會幫你改 `index.html` / `app.js`，改完你只要把新版檔案上傳回 GitHub（覆蓋舊檔案），Vercel 會自動偵測並重新部署，不需要再重新設定一次。

---

## 階段一的已知限制（之後階段二會處理）

- 「👍 讚」目前只是畫面上的互動效果，還沒有寫進資料庫、也還沒防止重複按讚（這是需求書「階段二」的項目）。
- 「這個價格我也確認過」按鈕、檢舉功能，都還沒加上（同樣是階段二項目）。
- 店家認證徽章的資料欄位已經建好，但還沒有申請/審核的介面（階段三）。

---

## 地圖功能小補充

地圖維持原本免費、不用金鑰的 Leaflet + OpenStreetMap 方案，圖層換成比較現代簡潔的 CARTO Voyager 樣式，並加了幾個優化：

- 品項在地圖上會用「藥丸形價格標籤」直接顯示金額
- 同一區域點位太密集時會自動合併成數字集群，放大地圖才會展開成個別標記
- 允許定位權限的話，地圖上會有一個藍點顯示你目前的位置
- 回報表單裡可以直接搜尋店名/地址，系統會幫你把圖釘移過去，不用自己在地圖上找位置

自己回報過的品項，卡片右下角會出現「🗑️ 刪除」按鈕，可以刪除自己的回報（別人回報的資料仍然不能刪除或修改，避免被亂改）。

如果階段一測試都沒問題，跟我說一聲，我們就可以進到階段二。

---

## 這次更新（畫面現代化 + Uber Eats 風格清單）

這次只改了 `index.html` 和 `app.js` 這兩個檔案，**不需要**重新設定 Supabase 或改 `config.js`。上傳回 GitHub 時，把這兩個檔案覆蓋掉舊的就好。

改了什麼：

1. **整體視覺改成主流 App 風格**：白底、圓角卡片、比較柔和的陰影，取代原本的收據票券樣式。價格標籤顏色也統一成：綠色（超佛心）、橘色（俗擱大碗）、珊瑚紅（划算頂規）。
2. **地圖改成滿版全螢幕**，並把下面的「品項清單」改成像 Uber Eats／外送 App 一樣，從地圖下方滑出來的「抽屜」：
   - 平常只會看到地圖下緣有一條窄窄的橫條，寫著「📋 附近餐廳列表」和目前的筆數
   - 點一下那條橫條，清單會往上滑出來，蓋在地圖上面，可以滾動瀏覽每一家的詳細資訊（店名、品項、價格、評分、留言、照片等）
   - 再點一次橫條（或點清單裡任何一筆的「📍 在地圖上看」），清單會收合回去，讓你看到完整地圖
   - 這樣手機上就不用一直往下滑頁面才能看到清單，跟地圖可以同時並存

如果之後想要清單預設就展開一部分（像 Uber Eats 那樣一開始就露出幾張卡片），或是想要用手指拖曳清單高度而不是用點的，跟我說一聲，我可以再幫你加。

---

## 這次更新（個人任務／積分儀表板 + 兌換商店 + 配色再調整）

這次**四個檔案都要重新上傳覆蓋**：`index.html`、`app.js`、`config.js`（沒改內容，但保險起見一起上傳）、`supabase-schema.sql`（**這次一定要重新去 Supabase 執行一次！**）。

### 你需要多做一個步驟：重新執行 SQL

1. 打開 Supabase 專案 → 左側「SQL Editor」→「New query」
2. 把新的 `supabase-schema.sql` 整份內容貼上去（跟第一次部署時一樣的做法，這份腳本可以重複執行不會出錯）
3. 按「Run」，看到 `Success. No rows returned` 就完成了
4. 這會新增一張 `user_points` 表，還有簽到／兌換用的兩個函式，是這次新功能的資料庫基礎

### 新增了什麼

1. **底部導覽多了一個「🎯 任務」頁籤**，裡面是「我的省錢成就」儀表板：
   - 頂部大大的積分卡片，可以「每日簽到」領 10 分（連續簽到會累計天數，但目前只記錄天數，還沒有加碼獎勵）
   - 回報一筆品項 +30 分，如果有附照片再多 +20 分（系統自動判斷、自動加分，不用手動點）
   - 兩個進度條任務：「標註 5 家新餐廳」「累積 7 筆 $100 以下品項」，會依照你自己的回報紀錄自動計算進度
   - 底部有「前往積分兌換商店」按鈕
2. **積分兌換商店**：目前放了 4 個示意商品（咖啡、滷肉飯、超商禮券 $50／$100），積分夠就能按「立即兌換」，系統會即時扣點。**這部分目前只有「扣點」的邏輯，還沒有串接真正的兌換／核銷流程**（例如產生兌換碼、跟店家合作對帳、防止重複兌換的實體憑證），這塊需要之後你決定要怎麼跟店家合作、怎麼核銷，我們再一起設計實際的兌換方式。現在的版本比較像是「先把積分制度跑起來，讓使用者有感覺」的階段。
3. **整體配色再調整**：改用比較柔和的霧灰藍、淡米色、灰綠這類低飽和度色系，卡片圓角加大，陰影更輕柔，價格標籤和地圖上的圖示也一併套用新配色。

### 提醒

- 積分是綁在你的登入身分（含匿名身分）上，如果之後用無痕視窗或清除瀏覽器資料，會變成新的匿名身分、積分歸零重算，這跟之前「無痕視窗不能刪除自己回報」是同樣的原理。
- 兌換商店目前的品項、所需積分都是我先幫你抓的示意數字，之後可以再依你實際想推的優惠調整。
